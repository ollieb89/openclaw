---
phase: 01-foundation-repo-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/mask-api-key.ts
  - src/utils/mask-api-key.test.ts
  - src/agents/tools/session-status-tool.ts
  - src/auto-reply/reply/commands-status.ts
  - src/auto-reply/reply/directive-handling.auth.ts
  - src/commands/models/list.format.ts
autonomous: true

must_haves:
  truths:
    - "API keys displayed to users or agents show only the first 4 characters plus total length"
    - "No API key display anywhere in the codebase shows trailing characters"
    - "Short API keys (<=16 chars) are still masked, not shown in full"
  artifacts:
    - path: "src/utils/mask-api-key.ts"
      provides: "Single canonical maskApiKey function"
      exports: ["maskApiKey"]
    - path: "src/utils/mask-api-key.test.ts"
      provides: "Tests for maskApiKey covering edge cases"
      min_lines: 20
  key_links:
    - from: "src/agents/tools/session-status-tool.ts"
      to: "src/utils/mask-api-key.ts"
      via: "import maskApiKey"
      pattern: "import.*maskApiKey.*mask-api-key"
    - from: "src/auto-reply/reply/commands-status.ts"
      to: "src/utils/mask-api-key.ts"
      via: "import maskApiKey"
      pattern: "import.*maskApiKey.*mask-api-key"
    - from: "src/auto-reply/reply/directive-handling.auth.ts"
      to: "src/utils/mask-api-key.ts"
      via: "import maskApiKey"
      pattern: "import.*maskApiKey.*mask-api-key"
    - from: "src/commands/models/list.format.ts"
      to: "src/utils/mask-api-key.ts"
      via: "import maskApiKey"
      pattern: "import.*maskApiKey.*mask-api-key"
---

<objective>
Unify and fix all API key masking to show only prefix + length (TOOL-02).

Purpose: The codebase has 4 duplicate masking functions, two showing trailing characters (`formatApiKeySnippet`) and two showing 8+8 chars for long keys (`maskApiKey`). Both violate TOOL-02 which requires `sk-pr... (52 chars)` format -- first 4 chars plus length, never trailing characters.

Output: A single `maskApiKey` function in `src/utils/mask-api-key.ts` used by all 4 call sites, with tests.
</objective>

<execution_context>
@/home/ollie/.claude/get-shit-done/workflows/execute-plan.md
@/home/ollie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-repo-hygiene/01-RESEARCH.md
@src/agents/tools/session-status-tool.ts
@src/auto-reply/reply/commands-status.ts
@src/auto-reply/reply/directive-handling.auth.ts
@src/commands/models/list.format.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified maskApiKey utility with tests</name>
  <files>src/utils/mask-api-key.ts, src/utils/mask-api-key.test.ts</files>
  <action>
Create `src/utils/mask-api-key.ts` with a single exported function:

```typescript
/**
 * Masks an API key showing only the first 4 characters and total length.
 * Output format: "sk-pr... (52 chars)"
 *
 * This is the ONLY function that should be used for masking API keys
 * in user-visible output (session_status, /status, models list, etc.).
 */
export function maskApiKey(apiKey: string): string {
  const compact = apiKey.replace(/\s+/g, "").trim();
  if (!compact) {
    return "unknown";
  }
  const prefix = compact.slice(0, 4);
  return `${prefix}... (${compact.length} chars)`;
}
```

Create `src/utils/mask-api-key.test.ts` colocated test file with these cases:
- Standard key: `"sk-proj-abc123xyz789longkey"` -> `"sk-p... (26 chars)"` (first 4 chars + length)
- Empty string: `""` -> `"unknown"`
- Whitespace only: `"  "` -> `"unknown"`
- Short key (4 chars): `"abcd"` -> `"abcd... (4 chars)"`
- Very short key (2 chars): `"ab"` -> `"ab... (2 chars)"`
- Key with spaces: `"sk proj abc"` -> `"skpr... (9 chars)"` (spaces stripped before masking)
- Long realistic key: a 52-char key starting with `sk-proj-` -> first 4 chars shown, `(52 chars)` suffix
- Verify NO trailing characters appear in any output

Use vitest (`import { describe, it, expect } from "vitest"`). Follow colocated test pattern.
  </action>
  <verify>`cd /home/ollie/Development/Tools/openclaw && bun run test:fast -- src/utils/mask-api-key.test.ts` passes all cases</verify>
  <done>maskApiKey function exists, returns correct format for all edge cases, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 4 duplicate masking functions with unified import</name>
  <files>src/agents/tools/session-status-tool.ts, src/auto-reply/reply/commands-status.ts, src/auto-reply/reply/directive-handling.auth.ts, src/commands/models/list.format.ts</files>
  <action>
In each of the 4 files, replace the local masking function with an import from the new shared utility:

1. **`src/agents/tools/session-status-tool.ts`** (lines 56-65):
   - Delete the local `formatApiKeySnippet` function
   - Add import: `import { maskApiKey } from "../../utils/mask-api-key.js";`
   - Find all call sites of `formatApiKeySnippet` in this file and replace with `maskApiKey`

2. **`src/auto-reply/reply/commands-status.ts`** (lines 35-44):
   - Delete the local `formatApiKeySnippet` function
   - Add import: `import { maskApiKey } from "../../utils/mask-api-key.js";`
   - Find all call sites of `formatApiKeySnippet` in this file and replace with `maskApiKey`

3. **`src/auto-reply/reply/directive-handling.auth.ts`** (lines 18-27):
   - Delete the local `maskApiKey` const
   - Add import: `import { maskApiKey } from "../../utils/mask-api-key.js";`
   - Call sites already use `maskApiKey` so no rename needed

4. **`src/commands/models/list.format.ts`** (lines 59-68):
   - Delete the local `maskApiKey` const
   - Add import: `import { maskApiKey } from "../../utils/mask-api-key.js";`
   - Call sites already use `maskApiKey` so no rename needed

After each replacement, grep for any remaining references to `formatApiKeySnippet` in the codebase to ensure none were missed.
  </action>
  <verify>
Run `bun run check` from the project root to confirm no type errors, lint errors, or format issues.
Then run `bun run test:fast` to confirm existing tests still pass.
Grep for `formatApiKeySnippet` across the entire `src/` directory -- should return zero results.
  </verify>
  <done>All 4 duplicate masking functions removed. Every API key display in the codebase uses the shared `maskApiKey` from `src/utils/mask-api-key.ts`. No instance of `formatApiKeySnippet` remains. Output format is `prefix... (N chars)` everywhere.</done>
</task>

</tasks>

<verification>
- `bun run test:fast -- src/utils/mask-api-key.test.ts` -- new unit tests pass
- `bun run check` -- no type/lint/format errors
- `bun run test:fast` -- all existing tests pass
- `grep -r "formatApiKeySnippet" src/` -- returns nothing
- `grep -r "\.slice(-" src/utils/mask-api-key.ts` -- returns nothing (no trailing chars)
</verification>

<success_criteria>
- A single `maskApiKey` function exists at `src/utils/mask-api-key.ts`
- All 4 prior implementations replaced with imports from the shared function
- Output format matches TOOL-02: `sk-pr... (52 chars)` -- first 4 chars, never trailing characters
- All tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-hygiene/01-01-SUMMARY.md`
</output>
